name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-mac:
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v4

      - name: Build with Swift Package Manager
        run: swift build -c release
